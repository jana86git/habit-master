import { colors } from "@/constants/colors";
export const styles = {
    task_container: {
        flex: 1,
        backgroundColor: colors.background,
        borderColor: colors.secondary,
        borderWidth: 1,
        borderRadius: 8
    },
    task_heading: {
        // fontWeight: "900",
        fontSize:18,
        color: colors.text
    }
}